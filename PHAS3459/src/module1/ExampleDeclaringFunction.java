/*
package module1;

public class ExampleDeclaringFunction {
	public static void main(String[] args) {
		
		
		// The function declaration is of the form:
		//
		// <return type> FunctionName (<argument list>)
		//
		// where:
		// <return type> : the type of the result returned by the function
		// (e.g. int, double or a class type).
		// FunctionName : programmer defined function name.
		// <argument list> : list of variables with their types passed to
		// the function, and used in the function code.
		double quadratic (double x) {
		double y;
		double a = 2.0;
		double b = 3.0;
		double c = 5.0;
		y = a*x*x + b*x + c;
		// The following line is how we send the value of the function
		// back to the code that called the function:
		return y;
		}
		
	}
}

*/