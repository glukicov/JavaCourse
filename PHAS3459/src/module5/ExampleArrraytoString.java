/*package module5;

public class ExampleArrraytoString {

	//Returning readable matrix as a string
			String matrix ="[";	
			//looping through rows
			for(int r = 0; r<array.length; r++){
				matrix +="(";
				//looping through columns
				for(int c=0; c<array[r].length; c++){
					matrix += array[r][c];
					if(c+1!=array[r].length){matrix +=",";}
				}
				matrix += ")";
				if(r+1!=array.length){matrix +=",";}
			}
			return matrix += "]";
	
}
*/